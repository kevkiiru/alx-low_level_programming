# include <stdio.h>
# include "main.h"

/**
 * print_alphabet - Entry point
 * Return: Always 0 (succes)
*/

void print_alphabet(void)
{
	char ch = 'a';

	for (a > 'a'; a <= 'z'; a++)
	{
		putchar(ch);
	}
	putchar('\n');
	return (1);
}
