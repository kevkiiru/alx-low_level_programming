# include<stdio.h>
# include "main.h"

/**
 * print_rev - main entry point
 * @s: main integer
 * Return: 0 always
 */

void print_rev(char *s)
{
	int a;

	for (a = 0; a[s] != '\0'; a--)
	{
		_putchar(a);
	}
	_putchar('\n');
}
